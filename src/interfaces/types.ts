// types file for initializing the type of event data

export interface EventData
{
  year: string;
  title: string;
  date: string;
  description: string;
  image: string;
  caption: string;
  category: string;
}
